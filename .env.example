# Optional. Set a server-side Anthropic API key to offer visitors a limited
# free tier. Leave unset to require bring-your-own-key in the UI.
ANTHROPIC_API_KEY=

# Free runs per IP per day when the server key is set. Default: 3.
FREE_RUNS_PER_DAY=3
